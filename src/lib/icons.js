/* Central icon map so data files can reference icons by string name
   and reusable components (CardGrid, etc.) can resolve them generically. */
import {
  Scale,
  Workflow,
  SlidersHorizontal,
  UserMinus,
  LayoutGrid,
  FileWarning,
  Compass,
  Users,
  UsersRound,
  Layers,
  Route,
  Sparkles,
  CalendarCheck,
  Target,
  ClipboardList,
  Map,
} from 'lucide-react'

export const icons = {
  Scale,
  Workflow,
  SlidersHorizontal,
  UserMinus,
  LayoutGrid,
  FileWarning,
  Compass,
  Users,
  UsersRound,
  Layers,
  Route,
  Sparkles,
  CalendarCheck,
  Target,
  ClipboardList,
  Map,
}
